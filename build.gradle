plugins {
    id 'java'
}

group = 'tr.xyz'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation('org.jetbrains:annotations:23.0.0')
    implementation project(':digit')
}

test {
    useJUnitPlatform()
}